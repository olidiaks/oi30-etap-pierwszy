#include <iostream>
#include <deque>
#include <list>
#include <math.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    //n – liczba wiosek w Bajtocji.
    //k – liczba królewiczów synów samego Bajtazara 16 – ego
    int n, k;
    cin >> n >> k;
    int vilaggesPaths[n][n];
    int villagesLongaPaths[n];
    return 0;
}
